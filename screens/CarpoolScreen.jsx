import { View, Text } from "react-native";

export default function CarpoolScreen() {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>I am the carpool screen</Text>
      </View>
    );
  }